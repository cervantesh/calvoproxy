// Package configenv provides environment helpers with explicit fallback alias
// support.
//
// This package must remain standalone and must not import project-specific
// packages.
package configenv
